import java.util.Comparator;

public class PartitionTable {
	Partition [] partitions ;
	int numPartitions;
	KV keyValue;

	public class addToPartitionAt(int index, Object key, int value){

		KV keyvalue = new KV(key, value);
		partitions[index].add(keyvalue);

	}
	public class sortPartitionAt(int index){

		partition.

	}
	public class isSortedPartitionAt(){

	}

}
